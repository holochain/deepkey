{
  "name": "tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "TRYORAMA_LOG_LEVEL=${TRYORAMA_LOG_LEVEL:-error} NODE_NO_WARNINGS=1 vitest --threads false --run -t ${TESTS:-.}"
  },
  "author": "",
  "license": "CAL-1.0",
  "dependencies": {
    "@holochain/client": "^0.14.1",
    "@holochain/tryorama": "^0.11.2",
    "@msgpack/msgpack": "^2.8.0",
    "@noble/ed25519": "^2.0.0",
    "js-base64": "^3.7.5",
    "vitest": "^0.31.0",
    "ws": "^8.13.0"
  },
  "type": "module",
  "devDependencies": {
    "vite": "^4.0"
  }
}